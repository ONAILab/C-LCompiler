int gcd(int u, int v) {
    if (v == 0) {
        return u;
    } else {
        return gcd(v, u - u / v * v);
    }
}


void {
    int x;
    int y;
    int result;
    x = 120;
    y = 45;
    result = gcd(x, y);
}